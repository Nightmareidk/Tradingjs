<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header>
    <h1><%= title %></h1> <!-- Displays "Collections - CardTrader" -->
  </header>

  <main class="container py-5">
    <div class="row">
      <% categories.forEach(category => { %> <!-- Loop through all categories -->
        <div class="col-md-4 mb-4">
          <div class="card">
            <div class="card-body">
              <h5 class="card-title"><%= category.name %></h5> <!-- Display category name -->
              <p class="card-text"><%= category.description %></p> <!-- Display category description -->
              <a href="/collections/<%= category.id %>" class="btn btn-primary">View Cards</a> <!-- Link to show cards of this category -->
            </div>
          </div>
        </div>
      <% }); %>
    </div>
  </main>

  <footer>
    <p>© 2024 CardTrader</p>
  </footer>
</body>
</html>
            